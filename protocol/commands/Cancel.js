import * as reader from '../reader.js';
import * as writer from '../writer.js';

const Cancel = () => {
  return { , bytes: [
,
 ]};
};

Cancel.code = 0x02;

Cancel.fromBytes = (bytes) => {
  return Cancel();
};

export { Cancel };
